# TeamExecutor environment variables (example).
# Copy to .env and fill in as needed. None are required for the library itself;
# agent backends (claude / codex CLIs) read their own credentials from the
# environment of the spawning shell.

# Optional: Anthropic API key, consumed by the `claude` CLI backend if configured.
# ANTHROPIC_API_KEY=
